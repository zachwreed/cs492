Setup for Dart application:
1.) Check that "dart-sdk" has been installed by running "$ dart --version"
2.) Cd to project directory and call "stagehand console-full" to create the boilerplates
3.) Run "pub get" to install dependenices in pubspec.yaml
4.) Call "dart bin/main.dart" to run